# Changelog

> 只记**用户可见**变化(功能 / 修复 / 安全 / 性能);内部债务与未来承诺只进
> [GitHub Issues](https://github.com/jinjunnn/alpha-code/issues)，不得写入历史
> `BACKLOG.md`。
> 写入时机：用户可见实现 PR 写入 `[Unreleased]`；实际发布时移动到日期和
> 版本标题。具体流程见 [Alpha documentation SOP](https://github.com/jinjunnn/alpha-work/blob/main/governance/documentation-sop.md)。

## [Unreleased]

### Added
- **扩展目录开始消费官方安全审核结论(地基半场:安装与启用策略先行)**(REQ-104 #397 PR-A,需下个签名版本生效)——签名目录里经过安全审核的扩展现在携带一份逐项审核结论(分级、启用方式、上游状态、复审期限、组件清单与来源快照的校验引用),本次先让**安装与启用面**真实消费它:①审核声明的启用方式优先于按来源猜的保守规则——声明「自动开启」的核心能力装完即用,声明「手动开启」的装完保持关闭;②标注「按会话试用」的实验室扩展,永远不会被持久开启:安装后保持关闭,任何路径尝试把它设为常开都会被如实拒绝(按会话开启的机制随后续版本提供),历史上已被设为常开的也会在下次启动时回到关闭;③上游仓库已归档的扩展停止提供新安装(已装的不受影响),套件里若包含此类必选子项则整包如实失败、可选子项跳过并说明;④安全复审已过期的扩展,新装先保持关闭、开启需要你显式确认;⑤审核数据但凡有一处对不上(未知字段、校验不过),整份一律不采信,按未审核的保守规则处理,绝不挑着用。组件清单与来源快照按需获取、逐字节校验后才会展示(展示界面随下半场交付)。审核结论如何呈现在推荐页与详情页(四级货架)也随下半场交付。
- **第三方扩展装完默认不启用,启用开关扩展到全部类型**(REQ-104 #395,需下个签名版本生效)——从目录安装非 Alpha 自产的扩展(包括 GitHub 这类"官方出品"的连接器:官方出品不等于我们审过,供应链语义按第三方)后,它出现在「已安装」列表但处于关闭状态:技能不注入、agent 不加载、插件不进加载名单、连接器不连接,行上有「已安装 · 未启用」徽标,安装完成的提示也如实说"已安装但未启用"。你打开行内开关才生效——开关现在对技能/agent/插件/连接器全类型可用(此前只有连接器有),关闭同样真实卸下运行面。Alpha 自产与出厂自带能力保持默认开;你自己导入的技能/agent 视为自选内容,同样默认开;已装扩展不回溯改状态,更新/回滚永远保留你当前设定的开关状态(禁用状态下更新照常换代内容,但绝不静默重新启用)。启停切换在同一把锁内原子写入配置投影(禁用的插件从配置缺席、连接器写 enabled:false、agent 写 disable:true)并翻转账本,配置自持禁用态、界面显示与实际运行状态一致;被安全公告点名的扩展重新启用仍会被拦下。
- **套件详情页新增「整包事实」段,装前看清整体代价**(REQ-104 #396,需下个签名版本生效)——套件(多个扩展打包一键装)的详情页在简介与子项列表之间新增一段整包汇总:下载体积(只统计经校验钉死的下载资产,含未钉死子项时如实显示「≥ 已知体积 + N 项体积未知」,绝不估算)、所需密钥变量、运行位置、支持等级(按全部子项的最低档),以及整包能力并集——每项能力标注来自哪个子项,高风险能力(本机子进程、引擎进程内代码)排前并显式标出,与安装时的授权确认框同一套派生来源与风险分级,两处永不打架。
- **「已安装」列表现在真实分「全局 / 本项目」两组**(REQ-099 #307,需下个签名版本生效)——打开某个项目的会话后,定制中心「已安装」页会出现「本项目」组,列出该项目导入或收编的本地扩展(全局组一切如旧)。项目组是只读的:只能卸载,没有启用开关,也没有装到项目的入口(受管安装始终装到全局,ADR-030);卸载严格按记录的作用域执行——全局与项目同名的扩展是两行、各卸各的,绝不会删错对象;项目上下文丢失时卸载如实拒绝而不是悄悄退回全局。项目组的健康三态与全局组同源(governance 只读查询现在带项目账本)。回到主页或无项目时,「本项目」组不显示;项目还没有本地扩展时,组头保留并说明项目扩展从哪来。
- **扩展详情页新增「已授权能力」段,授权总账按扩展就近可查**(REQ-103 #392,需下个签名版本生效)——任何已安装扩展的详情页,在「所有权」与「来源与签名」之间新增一段只读的能力授权记录:你在安装/更新确认框里授权过什么(注入上下文、写引擎配置、跑本机子进程、在引擎进程内执行代码等),这里逐条列出,风险分级与确认框同一套词汇(高风险能力显式标出),并注明授权时间与所属事务。这一段是纯查询面:没有任何按钮,能力变更仍然只经确认框发生;记录随安装提交生效、随卸载删除。早于授权闸(#348)装入的存量扩展如实显示「没有授权记录」,绝不显示按类型推测的"本应请求"清单;未请求任何能力的导入件也如实说明。
- **定制中心的扩展详情页现在展示完整的所有权与来源事实**(REQ-103 #195 S50 切片,需下个签名版本生效)——每个扩展的详情页新增五维所有权段(作者、策展方、分发渠道、运行位置、支持层级)与来源签名段(随包/远程目录/验签状态),已安装列表的状态徽章简化为「可用/已启用/健康」三态分离展示;内置(上游)能力面板与扩展清单正名分家(builtin-controls / ext-inventory),读面走独立的零写查询通道。

### Fixed
- **扩展装好后的授权记录若没写成,现在会如实告知并自动补齐,不再静默丢失**(REQ-100 #336,安全,需下个签名版本生效)——扩展安装成功提交后,还要落两笔授权记录(下次升级对比用的已授权能力集、本次授权的审计凭据)。此前这两笔写失败只在日志里留一行警告,界面照常报成功,而且永远没人重试——记录就此静默缺失。现在写失败会随安装结果如实带出,事务保持在「授权记录待补」状态,应用会在下次任何扩展操作前或重启时自动重试直到补齐;补齐前新的扩展写操作会被如实拒绝(说明原因,重试即可)。同时堵死一类更深的隐患:安装记录一旦写成,后续任何一步失败(包括恢复期的临时故障)都只会向前补齐,绝不再把已装好的扩展回滚成「账上已装、实际已卸」的错账。
- **导入/启用技能时若“启用名单”没刷新成功,现在会如实提示「重启后生效」,不再谎称当场生效**(REQ-104 #395 收口 / REQ-100 #336,需下个签名版本生效)——技能启用依赖一份随账本派生的启用名单;账本写成而名单刷新失败时(磁盘满等罕见故障),技能本次不会注入,重启后自动补齐。此前这种降级只在日志里可见,导入/启用界面仍报「当场生效」。现在安装、导入(文件夹/Git)、启用开关都会如实带出该状态,导入提示改为「已导入 · 重启应用后生效」;降级方向保持安全(名单只会偏严,绝不会把已停用的技能放出来)。
- **清理项目残留时部分条目失败,不再被包装成「清理成功」**(REQ-098 #372 收口 / REQ-100 #336,需下个签名版本生效)——项目残留清理逐项执行、互相隔离;此前哪怕有条目删不掉,整单仍报成功,失败清单藏在结果里容易被忽略。现在任何条目失败整单如实报失败,并附上已清理/未清理的完整清单;重试只补失败项,不重复动已清理的内容。
- **你固定保留的扩展内容不再可能被每日缓存清理误删**(REQ-102 #318 收口,安全,需下个签名版本生效)——扩展内容缓存的每日垃圾回收此前有两个「本该整轮拒绝、实际继续清理」的口子:①「固定保留」账本(pins.json)的格式漂移(版本号不对、缺版本号、保留表被写成数组等)会被当成「没有任何固定项」继续清理——你显式固定保留的内容可能被当垃圾删掉;②某个已装扩展的内容目录读不了(权限/磁盘错)时只记一条警告就继续清理——仅靠该扩展可达的缓存内容可能没被标记而被删,之后修复/重装就得重新下载。现在两处都严格按「宁可不清理,绝不误删」执行:固定保留账本只接受严格合法的格式,任何漂移、以及任何已装内容读不了/校验不了,当轮清理都整轮拒绝并如实记录原因(下轮自动再试);「从未固定过任何内容」「从未装过任何扩展」这类本来就允许的空状态照常通过,清理不受影响。
- **Alpha 页面崩溃后重新加载,现在无论怎么配置都会回到稳定的旧版页面**(REQ-088 #334,需下个签名版本生效)——此前只有「自动回退」模式的页面在致命渲染错误后会降级旧版;被显式配置成强制 Alpha 的页面(环境变量或本机 pin)崩溃后重新加载仍会回到同一个坏掉的 Alpha 页面,反复崩溃、没有兜底。现在只要本版本记录过该页面的致命渲染错误,重新加载一律回到旧版稳定页面,不再可能陷入崩溃循环;新版本发布后记录自然失效,页面照常回到 Alpha(手动恢复也仍然可行:清除该页面的崩溃记录即可)。崩溃页上的「重新加载」按钮也不再抢跑:它会先等错误记录真正写进磁盘(写入采用先写临时文件再原子替换,中途断电或磁盘错误不会留下损坏的记录文件)才可点击;万一记录保存失败,按钮如实显示保存失败并可重试,而不是假装已记录、把你送回同一个坏页面。
- **内置能力的「隐藏/恢复」等治理开关不再永久失灵**(REQ-103 S50 白捡存量 bug O4,需下个签名版本生效)——历史版本把治理记录物化在旧配置文件位置,真源迁移后这些「幽灵键」仍留在账上;任何一次应用治理变更都会因删除一个当前文件里不存在的键而整笔失败——所有治理开关(隐藏内置 agent、恢复出厂项等)从此永久按不动。现在幽灵键的删除会被安全跳过并自动出账(自愈),同一笔里的真实变更照常生效,用户自有配置零触碰。
- **单装 MCP 连接器/插件/云管线现在走事务通道,首装真正请求你的授权**(REQ-100 #378,需下个签名版本生效)——扩展目录里这三类的单个安装此前是各写各的直写路径:失败靠手工补偿兜底、中途崩溃可能留下半装态,而且不在能力授权闸内(装 MCP/插件/云从不弹授权确认)。现在与技能/agent 同一条事务安装链:配置、文件与账本在同一笔有日志的事务里落位,任一步失败或崩溃自动回滚到装前原样;首装或能力扩张会零副作用地停下弹授权确认,取消则不装不授权。连带修掉四个既有隐患:①带密钥的 MCP 更新失败(哪怕更新中途崩溃)不再可能毁掉旧安装正在使用的密钥文件——密钥改为只增不覆盖的版本化存储,旧文件直到新配置真正生效才被回收,更新失败旧连接器继续健康;②插件更新失败后旧版继续完整可用(文件、配置、账本三者原样);③目录 npm 插件安装前会同时核对新旧两份配置来源,不再可能把引擎合并视图里已存在的同名插件装成重复项;④云管线重装不再把你手动停用的状态静默改回启用,卸载云管线会同步清除其授权记录(重装重新请求授权,绝不静默继承)。
- **从 Git 导入技能只允许可信代码托管站,堵死内网探测面**(REQ-098 #335,安全,需下个签名版本生效)——此前「从 Git 仓库导入技能」的地址校验只挡协议与形状,却放行 `localhost`、内网/私网 IP、云元数据地址(169.254.169.254)与任意自定义端口:被攻陷的界面可诱导主进程向你的内网或本机服务发起请求(SSRF)。现在导入地址限制到产品策展的可信代码托管站白名单(github.com / gitlab.com / bitbucket.org / codeberg.org / git.sr.ht 及其子域),并用标准 URL 解析规范化后判定 —— 各种 IP 编码变体(十进制/十六进制/八进制)、IPv6、尾点绕过、用户名密码、查询串一律拒绝,只接受默认 443 端口;克隆时禁用重定向跟随、并隔离全局/系统 Git 配置(防止 `insteadOf` 之类的地址改写把可信站悄悄重指到内网)。白名单之外的自托管代码站暂不支持导入(后续会加显式配置项)。
- **每日扩展缓存清理不再可能冻结界面**(REQ-102 #367,需下个签名版本生效)——扩展内容缓存的每日垃圾回收(启动 5 分钟后首跑、此后每 24 小时一轮)此前整轮在主线程同步执行:清理耗时随已装扩展规模无界增长,实测重度安装(几十个扩展、含插件包)单轮 0.4-2 秒,期间窗口、快捷键与退出事件全部冻结。现在整轮清理在后台工作线程执行,主线程只收一份计数摘要,界面全程零阻塞;清理的全部安全语义原样保留(与安装事务互斥、单轮串行、锁心跳续租、任何存疑一律不删);后台线程启动失败不会退回主线程执行(那正是本次移除的行为),只是这一轮顺延到下个周期,空间清理晚一天无害;退出应用时不等清理收尾——清理被设计为任意时刻中断都安全,下一轮从头开始。
- **从目录安装 agent 现在走事务通道,并真正请求你的授权**(REQ-098 #361,需下个签名版本生效)——此前从扩展目录安装 agent(随包或远程)是三步分离的直写(md 文件、引擎配置、账本),失败靠手工补偿兜底,且不在能力授权闸内:装 agent 从不弹授权确认。现在目录 agent 与随包 seed agent(#358)走同一条事务安装链:内容一律先进验证共享缓存(远程按目录清单逐字节钉死,随包资产自算内容地址),md 文件与引擎配置条目在同一笔有日志的事务里一起落位,任一步失败或中途崩溃自动回滚到装前原样(文件恢复「不存在」而不是留空壳),绝无「文件在配置不在」的半装态;首次安装弹能力授权确认(注入上下文、写引擎配置),确认重驱走本地缓存复核、绝不二次下载,与技能同一套授权契约;同名 agent 已存在(装过、或你手工放过文件/写过配置)时如实拒绝覆盖,目录条目身份对不上(id 与名字不一致)也如实拒绝。#348 当时登记的「单装 agent 未进闸」缺口就此收掉;剩余 MCP/插件/云单装已由本版 REQ-100 #378 一并收编进闸(见上条)。
- **从文件夹 / Git 导入技能、从文件导入 agent 现在走事务通道,崩溃不再留半装态**(REQ-098 #390,需下个签名版本生效)——此前未策展导入(你自己的技能文件夹、Git 仓库技能、从 `.claude`/`.agents` 继承的生态技能,以及导入的 agent 文件)是逐文件直接复制、再单独记账:进程若在复制或落账中途崩溃,可能留下一个能被引擎加载的半成品技能,或一个已生效却没有账本记录的 agent(此后既查不到也修不掉)。现在这些导入与目录安装同一条事务安装链:内容先进验证共享缓存(按内容寻址、读取重验),文件与账本在同一笔有日志的事务里落位,任一步失败或崩溃自动回滚到装前原样,绝无半装态;同名已存在时如实拒绝覆盖(技能与 agent 均无就地更新,改内容走重导入)。相同内容的导入在不同环境间共享同一份缓存(去重),各环境的安装记录仍相互隔离。项目本地技能导入维持既有的 `<项目>/.alpha/skills` 路径不变。
- **插件更新不再有「旧已删、新未装」的失败窗口**(REQ-099 #352,需下个签名版本生效)——此前更新插件是两步独立操作:先卸载旧版、再安装新版,中间任何失败(配置忙、记账失败、崩溃)都会让插件直接消失。现在更新是一笔有日志的原子替换:配置里的旧条目与新条目在同一把锁内一步换掉、账本同锁落账,任一步失败自动回滚到旧版原样;中途崩溃下次启动自动收敛(重复恢复不产生重复账目)。替换前会严格核对旧安装事实(账本损坏、双重记录、账/配不一致、改名一律如实拒绝,绝不当作全新安装硬装);更新会保留你手动停用的状态,不会静默重新启用;版本没变时不做任何事。目录自带插件的新版本落在带版本号的独立目录,旧目录在成功后才清理——任何时刻磁盘上都有一份完整可用的插件。
- **项目自带的旧版扩展账本在打开项目时被自动收编**(REQ-099 #356,需下个签名版本生效)——此前只有全局账本会做 v1→v2 迁移,项目 `.alpha` 里的旧版安装记录(项目自带技能/agent 等)永远停留在旧格式:新的卸载/更新/冲突检查读不到它们的完整事实。现在打开项目时(无论项目是否含可执行扩展、是否已做过信任决策)自动把项目账本收编为 v2:身份按项目真实路径绑定,迁移在恢复收敛与项目锁内进行,损坏账本零改动如实报告,占用中自动改天重试。多渠道共用同一项目时,账本由先打开的渠道收编、各渠道一视同仁可读——渠道标记只是记录事实,不影响任何功能。
- **扩展目录单装(MCP/插件/agent/云)记账失败不再谎报安装成功**(REQ-100 #354,需下个签名版本生效)——此前这些类型装完后如果安装账本写入失败,界面仍然报「已安装」,只是带一条容易被忽略的警告;之后的卸载、更新、冲突检查都在一本失真的账上运行。现在记账失败 = 安装失败:已写入的配置/密钥/文件按写前快照精确复原(MCP 恢复旧配置与旧密钥、插件只撤本次写入的条目、agent 整撤本次全新安装 —— agent 自 #361 起改由事务引擎整体回滚),账本本身无需恢复(健康账本 + 原子写失败 = 磁盘零变化;刻意不做整文件恢复,那正是跨进程覆盖竞态的来源),补偿有任何缺口都会如实并入错误原因。连带收紧三个写前门:已在账的插件拒绝「覆盖式重装」(更新按钮现有的「先删后装」链路,其崩溃窗口由后续的原子替换通道 #352 收口)、配置里已有同名而账上无记录的内容不再被静默认领为目录安装、agent 已存在(有账、有文件或有手工配置)时直接拒绝覆盖。旧的「账本双写兜底」同步下线——账本从此只有一个写方,不存在两本账不一致的合法状态;损坏的账本在任何安装写入前被如实拒绝(原文件不动),不再有「写一半把旧账搬走」的路径。
- **扩展首装/扩权现在真的会请求你的授权**(REQ-100 #348,需下个签名版本生效)——引擎里早已建成的能力授权闸(装什么就要确认什么能力:注入上下文、写引擎配置、跑本机子进程、在引擎进程内执行代码等)此前在生产路径上形同虚设:安装计划从不携带能力集,确认对话框也不存在,首装与能力扩张全部静默通过、授权基线恒为空。现在:①单装技能、随包 seed 与套件安装把已验 manifest 的能力集真实写进事务计划;②首装或能力扩张时安装会**零权威副作用**地停下,弹出授权确认框——首装列全量请求,扩权分层标出「新增/已授权/将收回」,高风险能力(本机子进程、引擎进程内代码)显式警示;③确认即按「展示什么确认什么」的整集语义重驱安装(远端载荷走本地缓存复核,绝不二次下载),取消则不装不授权、静默返回;④授权记录与收据仅在安装成功提交后落盘,崩溃恢复自动补账,失败模式永远是「下次多问一次」而非静默继承;⑤批量更新遇到需要授权的条目会停下等你决定,不再一边弹框一边继续。单装 agent/MCP/插件/云均已由本版收编进事务通道(agent = REQ-098 #361,MCP/插件/云 = REQ-100 #378),全部目录安装路径都在此闸内。
- **扩展账本的中断残留当场收敛,不再等下次启动**(REQ-100 #347,需下个签名版本生效)——此前某次安装/卸载/回滚中断留下的事务残迹,要等重启才被收拾,期间后续操作在分叉的账本上继续写。现在每次装/卸/回滚/启停扩展前都会先把残迹自动收敛完再动手;收敛不了(在途事务、损坏残迹)就如实拒绝这次操作并说明原因(重试即可),绝不在对不上账的状态上硬写。启停开关也补上了并发保护,不再可能与进行中的安装互踩。
- **MCP 连接器卸载不再可能留下半拆状态**(REQ-100 #346,需下个签名版本生效)——此前卸载是三个独立步骤(删配置、吊销密钥、销账),中途崩溃会留下「配置没了密钥还在」「账上已删配置还在」这类不一致,且永远没人收拾。现在整个卸载是一笔有日志的事务:一把锁从头持到尾,任一步失败都如实报错并留下事务日志,下次启动自动接着把没删干净的收拾完(可重复、绝不谎报卸载成功);无法自动收敛的残迹(损坏/不可识别的事务日志)会如实保留并持续报告,留待显式诊断,绝不静默清掉。连带修掉两个恢复期潜在谎报点(缺恢复接缝时事务被错误标记为已完成)。旧的直删通道对有账连接器一律拒绝,只留给未登记的临时连接。
- **堵死一条「装了却不可用」的暗通道:扩展目录不再接受装进单个项目**(REQ-098 #372,ADR-030,需下个签名版本生效)——此前存在一条从未在界面开放、但底层接受的路径:把目录扩展装进某个项目的 `.alpha` 目录。装进去的技能引擎根本发现不了、装到一半崩溃永远没人收拾、也没有完整的卸载(会去账留文件)。现在这条路径在写盘前被如实拒绝(提示改用项目本地导入,该能力不受影响);历史遗留会在打开项目时如实报告,并提供显式、可重复的安全清理 —— 只清理账本能证明归属的内容,账本损坏、来历不明的目录、无账面依据的文件一律只报告不动手,有未收敛的事务残迹时整单拒绝,绝不自动删除任何东西。
- **扩展公示(advisory)真正拦住"禁止再启用"**(REQ-101 #314/#315,需下个签名版本生效)——扩展目录的签名信任链升级:目录、信任表、公示表与一致性快照互相钉死(任何一环被篡改/回放/删除都会被整套拒收),校验失败绝不再静默退回旧的弱校验通道;官方对某个扩展发布安全公示后,新安装、重新启用、回滚到旧版本都会被如实拦下并说明原因(已在运行的不强杀、卸载不受影响),公示撤回后自动放行且理由永久留档。
- **回滚老版本期间的新增技能/agent/插件,升级回来后不再凭空消失**(REQ-098 #304,需下个签名版本生效)——此前一旦升级到环境隔离版,再回滚旧版继续用(旧版直接读写 `~/.alpha`),期间新装的技能等状态在重新升级后永远不会出现在新版里(迁移只做一次)。现在每次启动会对账旧布局:回滚期新增的条目自动接回环境根;你在新版里改过/删过的东西绝不被覆盖或复活,配置文件的分歧只如实提示不自动合并。同时收紧一处隔离残口:迁移不再照搬指向旧布局的 symlink(绝对链/出树链/断链一律拒收并记账),环境内不会再有暗通旧根的引用。
- **模型选择框不再「已配置的供应商凭空消失 + 代理模型全灰」**(REQ-083,需下个签名版本生效)——引擎重启的间隙(存 Key、登录、激活代理都会触发引擎重启)打开模型选择框,此前会静默渲染成一个假象:已配置 Key 的 DeepSeek/智谱整体不可见、代理模型明明是会员也全部灰锁,而且点灰行还会再触发一次引擎重启,越点越坏。现在:①连不上引擎时如实显示「正在连接引擎…稍后自动恢复」,已配置的供应商保留占位行(「已配置 · 模型加载中…」),绝不消失;②弹窗自动重试,引擎一恢复列表就地补全,无需重开;③引擎不可达时点灰行不再触发重启(只有「引擎在线且代理确实未激活」才会激活代理)。真机断言:两类断网场景(拦截取数/真杀引擎)点灰行全程零重启。

### Added
- **随包 seed 的安装通道补齐 MCP 连接器与插件两类**(REQ-102 #359,需下个签名版本生效;真实随包 MCP/插件资产待产品策展后随 seed 快照同步,本版本先交付链路能力)——连接器安装 = 一笔有日志的配置事务:配置语义从随包目录条目派生并过与在线安装同一套安全门(命令白名单、禁内联执行、URL 白名单、危险环境变量拦截),需要密钥/工作区授权/Excel 沙箱的连接器如实拒绝(离线通道没有授权输入口);诚实边界:离线完成的是「配置安装」,本地 npm/uvx 型连接器首次运行仍可能联网拉包。插件安装 = 离线字节、配置与账本在**同一笔有日志的事务**里落位(逐文件校验,任意崩溃点要么全量回滚要么全量前滚;目录内容寻址、同内容同目录、重试不累积垃圾);已装旧版本走与在线更新同一套原子替换(任意时刻磁盘上都有完整可用的插件),已装更高版本如实拒绝降级。两类都过能力授权确认,卸载时授权记录随之清除(重装重新请求授权,绝不静默继承)——这条清除合同同时补到了 agent/插件/连接器全部卸载通道。
- **随包出厂的 agent(bug-triage)现在可以一键安装了**(REQ-102 #358,需下个签名版本生效)——此前随包 seed 里的 agent 资产只能浏览不能装(安装通道首期只开了技能)。现在 agent seed 走与技能同级的事务安装链:md 文件与引擎配置条目在同一笔有日志的事务里一起落位,任一步失败或中途崩溃自动回滚到装前原样(文件恢复「不存在」而不是留个空壳,配置恢复旧值),绝无「文件在配置不在」的半装态;安装内容与随包目录逐字节交叉核对,对不上如实拒绝;首次安装会弹能力授权确认(注入上下文、写引擎配置),与技能同一套授权契约,卸载后重装会重新请求授权而不是静默继承;同名 agent 已存在(装过、或你手工放过文件/写过配置)时如实拒绝覆盖。
- **首页与新会话页升级为 Alpha 正式页面,告别「覆盖层」形态**(REQ-084/085/086,需下个签名版本生效)——此前 Alpha 首页是盖在旧首页之上的覆盖层:旧页面仍在底下完整运行,双页面并存、`/new-session` 也被首页覆盖。现在首页与新会话页经 typed surface 接缝成为**唯一挂载**的正式页面:URL、历史 deep link、草稿生命周期与首条消息行为完全不变;新会话页支持 `?prompt=` 预填(只消费一次,刷新不重复);并新增故障保险——页面渲染致命错误时如实提示,确认重载后自动回退旧版页面,不再可能整屏卡死。
- **新增出厂技能「cloud-dispatch」:会话里就能把任务派到云上跑**(REQ-082,需下个签名版本生效)——登录(代付)状态下,让 agent「云端调研一下 X」即可正确派发:技能教会模型云任务的完整契约(调研/代码审查/文档三类管线与沙箱档、预算上限、结果取回),并内置数据边界纪律(代码只送 diff、绝不带密钥/.env)。未登录或 BYOK 时如实告知云派发不可用并指路登录,不会假装派发。
- **新增出厂技能「office-docs」:做表格/写文档/生成 PPT 有了正确指路**(REQ-080,需下个签名版本生效)——问「帮我做个 Excel」时模型知道该用哪个连接器(读文档=markitdown、写 xlsx/docx/pptx=对应办公连接器)、没装时指路扩展市场一键安装(或征得同意后用本地 Python 脚本兜底)、内置表格制作惯例(真实数字/日期类型、表头冻结、公式而非写死数值)与 PDF 生成代码片段(生态没有可信的 PDF 写连接器,如实补位不硬装)。
- **扩展市场上架办公三连:Excel / Word / PPT 读写连接器**(REQ-080,云端已生效,无需更新 app)——连接器 tab「办公」组新增三个可一键安装的写作连接器(全部 MIT、钉版、免装 Office;Word 的「转 PDF」单项工具需本机 Word,详情页如实标注);「办公套件」随之补齐为五件(读取 + 文件系统 + 写作三连),一键装齐。三个连接器的启动命令均经真实握手冒烟验证。
- **customize-alpha 技能补上连接器/套件章节**(REQ-082)——会话里问「怎么装连接器」不再只会指路定制中心:密钥为什么要走安装弹窗(密文存储,绝不明文写配置)、项目级注册的信任门语义、首跑下载慢是诚实状态、套件与插件的区别,一次讲清。
- **输入框支持真·附件:图片与 PDF(粘贴 / 拖拽 / 选择器三通道)**(REQ-078,需下个签名版本生效)——@/+ 弹窗「添加附件」现在打开真正的文件选择器;截图可直接粘贴进输入框、文件可拖进来,附件以缩略图标签展示、可单个移除,随消息真实发给模型。不支持的类型(文本/代码文件)与超限(图片 5MB / PDF 10MB)会如实提示原因并指路 @ 引用,绝不静默丢弃——此前弹窗里的「文件和文件夹」选了文件其实**发不出去**(首页点击无反应、会话页选中的文件被静默吞掉),本次一并根治。
- **@ 弹窗打开即看到本项目的改动文件**(REQ-078,需下个签名版本生效)——不输入关键词时「文件」节自动钉出 git 变更文件(最多 10 条,点击即引用);输入关键词照常全项目搜索,且搜索结果上限从 8 条放宽到 20 条。

### Changed
- **扩展市场来源标签消歧:「自建」只留给你真正自建的**(REQ-079 拍板补丁,需下个签名版本生效)——alpha 官方出品的精选条目(agent 精选卡、云连接器、来源筛选)此前统一标「自建」,像是用户自己造的;现改标「alpha 出品」,「自建」只出现在你会话内创建的 agent 上。
- **定制中心 Agent 页不再平铺引擎内置 agent**(REQ-079,需下个签名版本生效)——此前 build/plan/general 等内置 agent 占据浏览面主体(不可安装、不可删,点了也只能看),与「浏览 = 挑可安装的」心智相悖。现在 Agent 页只展示可安装的精选与你自建的 agent;内置 agent 的查看与管理(隐藏/停用/覆写)集中在 已安装 → 内置 治理面板,页面顶部有明确指路。
- **「附加终端」改为如实的「打开终端」**(REQ-078,需下个签名版本生效)——原文案称「把终端输出带进上下文」但实际只是打开终端面板(引擎没有该能力),现文案如实并注明「输出不会自动进入上下文」;首页(该处无终端面板)不再显示这一行。

### Added
- **Windows 版起步:出包链就位 + 核心功能对齐(实验性,真机验证中)**(REQ-076 T1/T2,ADR-026)——产品桌面平台扩为 macOS + Windows:新增 `ship:windows` 一键出未签名 Windows 安装包(NSIS);Windows 上补齐应用菜单(数据备份/清除/卸载说明 + 「帮助」内关于与开源声明);扩展市场装连接器前的运行工具检测(node/python/uv/git)在 Windows 正确判定(此前恒报「未安装」);「在编辑器打开」映射到 Windows 版 VS Code/Cursor 等;打包版安全头(CSP)对 Windows 同步启用;数据落点与 mac 完全同构(`~/.alpha`、`~/Alpha`、项目 `.alpha`)。**如实声明**:Windows 真机运行验证、正式签名(SmartScreen)、密钥文件的 Windows 权限加固均未完成(T3),当前包供内测;DB 安全带(备份/水位拦截)在 Windows 暂不可用(明示禁用非静默)。**正式构建通道已就位(REQ-077)**:CI 一键产出**含完整原生件**(终端 PTY / 文件监视)的 Windows 安装包,并内置完整性断言(缺件即红);mac 本地交叉包仅供开发冒烟。
- **@ 和 + 现在打开同一个"装配"弹窗;计划模式一键开启**(REQ-073,需下个签名版本生效)——输入 `@` 或点 `+`,同一个弹窗按节呈现:添加(文件和文件夹 / 附加终端 / **计划模式**)、Agent(@general/@explore 中文一句话简介 + 内置/个人标识)、文件(输入即搜)、扩展(一行直达扩展市场;此前"文档/PDF/表格/连接器"四行点谁都是同一动作,已收敛)。开启计划模式后输入框旁出现可关闭的「⊗ 计划」标签、提示语切换,发送自动走 plan 档(先出方案再动手);点标签、再选一次或按 Shift+Tab 都能关,新会话默认回 build。工具条上原来的「build」组件已移除——默认就是 build,不需要一个永远只有一行的下拉。记住分工:**`/` 执行,`@`/`+` 装配**(引用、附加、模式)。
- **键盘选中不再被鼠标"吸走"**(REQ-072/073 连带根治)——列表弹窗每次刷新时,若鼠标恰好悬停在弹窗上,浏览器会在原地合成一次鼠标事件把键盘选中拽回悬停行(表现为↑↓偶尔"失灵")。现在只有鼠标真移动才会改变选中。
- **`/` 命令菜单大修:键盘能用了、技能全都在、搜索真的搜**(REQ-072,需下个签名版本生效)——三处硬伤根治:↑↓ 键盘选择此前按了没反应(选中被内部刷新瞬间重置),现在可循环选择且滚动跟随;技能列表此前只显示前 12 条(排后面的技能永远看不见,得碰运气搜首字母),现在全量可见滚动可达;搜索此前只认命令名,现在名称与简介都搜(搜「审查」能找到 /review)、前缀命中排前、搜不到时如实显示「无匹配命令」而不是弹窗消失。展示随之重排:按类型分节(内置命令/技能/项目/MCP)+ 行首类型图标;来源改为行尾对齐的小字(内置/个人/项目/MCP,「个人」= 你自己装的,淡紫标识);出厂与内置命令简介转中文,第三方技能保留作者原文不机器翻译;底部常驻键位提示与「@ 引用 Agent / 文件」。新增 `/agents` 直达 Agent 管理;记住这个分工就够了:**`/` 执行,`@` 引用**。
- **新增默认工作目录 `~/Alpha`,不选项目也能直接开始**(REQ-071,需下个签名版本生效)——全新用户首页不再是「请先选择工作区」死点:未选项目时工作区默认落在个人目录下的 `Alpha` 文件夹(Finder 可见,首次真正使用时才创建;已有项目的用户不受影响,工作区菜单里也常驻「Alpha · 默认工作区」一键可切)。所有「选择目录」对话框默认定位到这里;自动化新建任务的目标目录也默认是它。已存在同名文件夹时如实沿用,绝不覆盖。
- **总结与记忆有了规范落点(Journal / Memory / Outputs)**(REQ-071,需下个签名版本生效)——新增出厂技能 **alpha-workspace**:会话里说「总结一下今天」「记住 X」,内容按约定写进 `Alpha/Journal/日期.md`(同日追加)与 `Alpha/Memory/`(一事一文件,可自己编辑);自动化面板新增「每日总结」模板(一键预填每天 21:00 写日报,保存与启用仍由你确认,默认不开);以 `~/Alpha` 为目标的云任务/自动化,其报告等交付物额外镜像到可见的 `Alpha/Outputs/日期-任务号/`(引擎内部记录仍在 `.alpha`,不重复展示)。

### Changed
- **市场下架「中国办公三件套」**(REQ-081,云端已生效,无需更新 app)——飞书 / 语雀 / 钉钉三个连接器与「中国办公套件」暂时退役(接入形态重新设计中,后续可能以更完善的方式回归);已安装的用户不受影响(启停/卸载照常),仅市场不再提供新装入口。离线时内置目录仍会短暂显示旧条目,随下个 app 版本消失。

### Fixed
- **BYOK 智谱 GLM / MiniMax「测试通过、会话却没反应」根治**(REQ-074,需下个版本生效)——两层问题一并修:① 配置供应商时「测试连接」与真实会话此前走的是**两个不同的 URL**(测试打对了、会话打错了;智谱网关对错误路径返回 200 包错误体,界面与日志双双无报错,表现为发消息后毫无动静),现在测试与会话严格同一拼法(测试通过 = 会话能用);② 引擎对「目录声明的模型」与「从公共模型库合并进来的模型」(如 glm-5.1)走不同的协议栈,anthropic 兼容接入点只对前者成立——内置供应商全部改走各家 **OpenAI 兼容端点**(智谱 `…/api/paas/v4`、MiniMax 官方现行域名 `api.minimaxi.com/v1`,均实测可用;旧 `api.minimax.chat` 配置两条路全 404,之前测试也不可能通过),两类模型自此协议一致。DeepSeek / 通义 / Kimi 全量核查无恙。自定义「Anthropic 兼容」节点的表单现注明:Base URL 填到 `/v1` 结尾。
- **窗口收窄时输入框跟手缩放,不再溢出窗口被裁**(REQ-075,需下个版本生效)——审查面板开启(或开过)后,会话栏宽度是一个记忆值,窗口缩小时不重新计算:输入框连同模型选择按钮被推出窗口右缘(看得见一半、点不着),要一直缩到很窄才突然恢复。现在会话栏永不超过窗口可用宽度,缩放全程连续跟手;审查面板在空间不足时自动让位。
- **未登录不再默认选中「登录才可用」的代理模型**(REQ-069,需下个签名版本生效)——此前未登录启动,输入框可能默认(或沿用上次登录期的选择)停在会员专属代理模型上,发第一条消息就被「预授权拒绝: member-only model 需 active 会员」原文糊脸。现在启动时按序解析:上次使用的模型(先校验当前还能不能用)→ 登录且账户可用时的代理默认档 → 已配 KEY 的自带供应商 → 都没有则保持「选择模型」引导。登录期的选择在登出后**暂停而非删除**(选择器里如实说明原因,重新登录自动还原);即使状态漂移,发送前也会先行内引导(登录或换自己的 KEY 模型),不再把网关拒绝原文当第一道界面。
- **未登录时模型选择器不再逐条展示海外模型**(REQ-069,需下个签名版本生效)——GPT / Claude 等代理模型的具体型号此前未登录也全量列出(全部锁定、点了只能去登录,观感是一排点不了的墙)。现在未登录时代理区收敛为一张「登录后可用 GPT / Claude 等海外模型 · 零配置」引导卡,DeepSeek / 智谱 / 通义 / Kimi / MiniMax 等自带 KEY 的国内供应商照常显示可配。
- **网关与云任务接口切换到自有域名**(REQ-070,需下个签名版本生效)——模型代理与云任务的默认接入点从 `*.workers.dev` 迁移到 `*.tidelabs.click`(境内可达性显著更好;`workers.dev` 存在广泛的 DNS 污染/连接重置报告)。旧地址在迁移窗口内保持在线,已登录用户的接入点也会随下次登录自动刷新。

### Added
- **其它工具的目录内容不再未经确认进入模型上下文**(REQ-063,需下个签名版本生效)——此前打开任何项目,其自带的 `.claude/`、`.agents/` 技能与 `CLAUDE.md` 会被静默读入(陌生仓库的提示注入面);现在默认不读取。项目自带这类内容时首次打开会询问:「导入」= 转换为该项目 `.alpha` 下的原生扩展(快照,原文件不动,可卸载可溯源);「忽略」= 保持不可见,按项目记录一次不再重复问。存量用户升级后首次启动会一次性询问是否把 `~/.claude`/`~/.agents` 里的技能与全局指令导入为 alpha 原生(不导入则明示从此不可见,原文件保留);之后随时可在会话里说「导入外部技能」(新出厂技能 integrate-project)补做或刷新快照。逃生开关 `ALPHA_ECOSYSTEM_INHERIT=1` 整机恢复旧行为。——此前模型常自称「alpha code (opencode)」、并把你指向 opencode 官方文档和 GitHub 仓库(对 alpha-code 用户全部失实)。现在发给模型的系统提示词在运行时完成品牌转写(底座工程能力原样保留,只转自我指代;你项目里提到 opencode 的文件名、包名、路径等真实事物一概不动);`/init` 与 `/review` 两个内置命令的模板、常驻子 agent(general/explore)的指令内容也改由 alpha 自有文本承载。新增出厂技能 **customize-alpha**:会话里问「怎么定制」即可获得 `.alpha` 配置、连接器、技能、治理等完整指引。逃生开关 `ALPHA_PROMPT_REBRAND_DISABLE=1` 一键恢复原样。
- **斜杠菜单更干净、每条命令自报家门**(REQ-066)——在治理设置里禁用的技能不再出现在 `/` 菜单(此前带「(已禁用)」占位仍列出);解禁后无需重启,下次打开菜单即恢复。菜单每条命令新增来源徽标:`内置` / `技能` / `项目` / `MCP` / `导入`,一眼分清命令来路;手动键入已禁用命令全名仍会收到诚实的禁用说明,不会假装执行。
- **项目里的一切定制只住 `.alpha` 一个目录**(REQ-060,需下个签名版本生效)——在会话里创建的技能 / Agent / 命令 / 连接器 / 插件全部落到项目的 `.alpha/` 下,项目里**不再出现 `.opencode` 目录**;没用过扩展的项目连 `.alpha` 都不会出现。模型经新的注册工具写入(字段校验、原子写、自动生效),不再手改配置文件;写完当前回复结束后自动重载,下一条消息即可用,无需重启。配套:全局安装 Agent 也不再在 `~/.opencode` 留任何痕迹(「`.alpha` 是你的,`.opencode` 是引擎的」自此全兑现)。
- **打开自带可执行扩展的项目会先征得你同意**(REQ-060 信任门,需下个签名版本生效)——项目若自带连接器(MCP)或插件(均为可在本机运行的程序代码),首次打开该项目会话时弹窗列明具体条目,你可以选择「允许加载」(需勾选知情确认)或「仅文本扩展」;技能 / Agent / 命令等文本类扩展不受影响。决定按项目记录一次不再重复询问,同意后无需重启即刻生效;拒绝后该项目的可执行扩展保持不加载。项目里放置未打包的 TypeScript 插件会被如实拒载并在日志说明原因(桌面运行时无法执行,需先打包成自包含 JS)。

### Fixed
- **「打开项目…」选完目录直接切换工作区**(REQ-068,需下个签名版本生效)——此前首页工作区菜单里选「打开项目…」,目录选完界面毫无反应(工作区还停在原项目,新项目也不出现在列表里),等于进不了新项目。现在选完立即切换到所选目录,取消则保持原样。
- **`~/.alpha` 与配置文件里不再出现你没装过的东西**(REQ-065/REQ-067,需下个签名版本生效)——出厂内置的技能此前以链接放在 `~/.alpha/skills/`、路径和"出厂禁用"还以明文写进你的配置文件 `alpha.jsonc`(系统件混进用户领地)。现在全部内置化:`~/.alpha` 只存放**你自己**安装/创建/导入的内容,配置文件只记录**你自己的**连接器/技能/Agent 和你的治理决定;出厂技能与出厂禁用项由 app 启动时在内部生效,磁盘零痕迹(历史残留升级后首次启动自动清理,你自装的内容绝不触碰)。
- **模型弹层里点「已配置供应商」不再整层关闭**(REQ-061)——此前点已配置的供应商行想改 KEY,弹层会直接消失,改 KEY 的入口永远进不去(表单「返回」同样误关)。根因是点击判定在界面同步刷新时误把层内点击当成了层外点击,已修复;弹层外空白处点击照常关闭。
- **会话页输入框按钮"点了没反应"根治**(REQ-056)——三个叠加原因一并修复:①接管容器层级低于对话滚动区,按钮**看得见点不着**(点击被透明滚动层吃掉);②窄窗口/审查面板打开时底部按钮行溢出输入框外,推理强度/发送被挤到不可点(现模型名截断省略、按钮行自动换行);③会话页丢失 alpha 焦点样式作用域,点击后残留上游橙色粗焦点圈(现样式随组件走,两页一致)。
- **推理强度不再有"选择模型后可用"的死状态**(REQ-056)——登录且代理可用时自动选中默认模型 **Claude Sonnet 4.6**(高级档;不覆盖你的手动选择、不写死到本地),推理强度点开即「默认/低/中/高」;模型不支持档位时点开如实说明原因,任何状态点击都有反馈。
- **自带 KEY(BYOK)供应商在新模型选择器里"消失"**(REQ-056)——未配置 KEY 的供应商此前被整行隐藏且无处补配;现在如实显示「需 KEY · 点击配置」直达配置表单,并补回「＋ 添加自定义节点/供应商」入口。注:正式包与本地测试包的数据目录相互独立,此前在测试包里填过的 KEY 需在正式包里重填一次。

## [0.1.2] - 2026-07-07

### Changed
- **输入框(composer)全面统一为 alpha 自建组件**(REQ-055)——此前首页与会话页是两套实现(首页自建、会话页为 opencode 原生加注入),反复出现行为不一致:首页推理强度点了没反应、模型按钮在无工作区时点击无反馈、agent 下拉泄漏内部专用档(alpha-automation 等)、点击后残留粗焦点圈。现在两处渲染**同一个组件、同一份样式**:模型/推理强度/agent/权限为本地选择、提交时作为显式参数发给引擎(不再遥控隐藏控件),推理强度即点即生效且跨页面一致;agent 列表只显示可用项(内部专用档彻底隐藏);会话页补齐上下文用量环;无工作区时所有控件引导先选工作区,不再有点了没反应的死按钮。诚实边界:附件/拖拽/图片粘贴暂未迁移(+ 菜单能力不变)。

### Fixed
- **首页模型/推理强度按钮死点**(REQ-054,随上条根除)——零工作区(全新安装首启/清除数据后)模型按钮点击无反应、推理强度对有档位模型切换不生效的问题,随 composer 统一重构从机制上消除。

## [0.1.1] - 2026-07-07

### Added
- **首次把项目派发到云端会先征得你同意**(B16 隐私合规,需下个签名版本生效)——每个项目**首次**使用云执行时,会弹窗说明将发送什么(本次的代码差异或任务文本)、发往哪里、可以拒绝,同意后按项目记录、不再重复询问;拒绝则不发送。配套:登录页明确告知"平台代付模式下对话内容会经平台处理"并提供自带密钥(BYOK)的替代方式,官网隐私政策补充了对话内容与云执行的数据处理专章(并修正了此前"不上传源代码"的不准确表述)。
- **技能市场的 Agent 也能远程上架安装了**(REQ-046,需下个签名版本生效)——此前只有连接器(MCP)和技能支持云端上架,Agent 必须随 app 打包;现在运营方在云端上架的 Agent,联网即见、点装即用(下载校验钉死,与技能同一安全管线)。自此**新增连接器/技能/Agent/插件都无需更新 app**:市场清单与内容统一由云端下发,app 内置清单只作断网兜底(发版时自动同步,不再手工维护)。
- **「数据 ▸ 清除数据…」**(C16,需下个签名版本生效)——app 内一键清除自己的数据,分两级:**仅凭证**(删除登录 token、BYOK 密钥、连接器密钥并登出,会话保留;删除范围逐项列出后确认);**全部数据**(为卸载做准备:应用数据、全局安装物 `~/.alpha` 及桥接链接、可勾选的引擎数据——会话数据库与独立 opencode CLI 共享,明示后由你决定;先提示导出会话数据库,红色终确认列出体积,清除后退出)。绝不触碰你的项目文件与 `~/.opencode` 里你自建的内容;逐项结果留痕日志。配套 [卸载与残留清单文档](docs/runbooks/uninstall.md)(菜单内「卸载与数据残留说明」直达)。
- **技能市场补货三条技能 + 「设计套件」回归**(REQ-045;远程市场上架,联网即见,无需更新 app)——此前撤下的「MCP 构建指南 / 视觉海报设计 / 品牌规范应用」内容就绪,重新上架:内容再分发自 Anthropic 官方示例技能(Apache-2.0,来源与许可声明随内容一并下载,下载逐文件校验);「设计套件」随两个成员齐整回归,「开发套件」补回「MCP 构建指南」可选成员。注:「视觉海报设计」含设计字体约 5.4MB,安装时下载。
- **技能市场上架两个新连接器**(E2/E6;远程市场上架,联网即见,无需更新 app)——**钉钉**(官方发布的 dingtalk-mcp:通讯录/机器人消息/DING 等;详情页如实标注其源码不可公开审计的供应链警示)与**数据库(DBHub)**(Bytebase 官方:PostgreSQL/MySQL/SQLite 等只读查询;固定为仍支持只读开关的版本,连接串按密钥采集、不落明文)。钉钉同时并入「中国办公套件」。

### Changed
- **平台模型清单收口**(REQ-030,云端已生效):国内版默认只显示国内系当代模型(DeepSeek V4 两档);DeepSeek 旧代(Chat/Reasoner)下架,存量会话/默认模型自动降级到 V4 对应档不报错。模型清单改为云端配置文件单一真源,后续增删模型不再需要发新版 app。
- **平台模型欠费自动切换**(REQ-031,云端已生效):运营方某家模型账户欠费时,同一模型自动切换到备用通道(OpenRouter)继续服务,计费按实际通道价目;全部通道不可用时如实报错,不静默。
- **「创建技能/Agent」从表单改为对话式**(REQ-036,需下个签名版本生效)——定制中心的裸表单(要求手写 markdown)已移除;现在直接在会话里说「帮我创建一个技能/agent」,出厂内置的 skill-creator 与 agent-creator 会访谈、生成并写盘,创建完成后当前会话下一条消息即可用(免重启)。「导入」(文件夹/git/npm)保留并成为该 tab 的主语义;技能市场中出厂内置的条目显示「出厂内置」而非「安装」。逃生开关:`ALPHA_FACTORY_SKILLS_DISABLE=1`。

### Fixed
- **出厂技能改为落 `~/.alpha` 真源,`.opencode` 里只放指针**(REQ-052,需下个签名版本生效)——此前出厂内置的 skill-creator / agent-creator 以 symlink 直接放在 `~/.opencode/skill/` 指向 app 安装包,绕过了「alpha 的东西都在 `~/.alpha`」的约定,打开 `~/.opencode` 会看到两个来路不明的条目。现在与市场安装完全同构:真源落 `~/.alpha/skills/`(仍是零拷贝链接,不占空间),`~/.opencode` 里只有指回 `~/.alpha` 的桥;旧位置的链接启动时自动迁移(只动 alpha 自己创建的链接,你自建的同名内容绝不触碰)。
- **国内版的云任务/云定时任务不再必然失败**(REQ-039,云端已生效)——此前云端执行任务(调研管线、定时任务)内部使用的模型被国内版模型白名单误拦,国内用户派发的云任务一律报错。现在服务端内部调用不再受该白名单限制(白名单本意只管你能在模型列表里选什么);你能看到、能选的模型完全不变,计费额度控制也不变。云端优先改用国产模型执行(成本与数据流向优化)已另行立项。
- **技能市场不再每次上新就把所有已装条目误标「可更新」**(REQ-048,云端已生效)——市场清单每次上架新条目都会更新总版本号,此前已安装条目会因此全部亮起「可更新」角标(内容其实没变,点更新也只是原样重装)。现在每个条目带独立版本号,只有该条目内容真的更新才会提示;发布工具同时加了防复发守卫(漏写条目版本会直接拒绝发布)。注:此前已装条目的记录里存的是旧式总版本号,这批安装对未来的真实更新提示不敏感(重装一次该条目即恢复);当前装机面仅开发机,随真机批顺带处理。
- **扩展安装不再可能静默写进早已失效的临时目录**(REQ-047,需下个签名版本生效)——app 缓存「登录 shell 环境」时会把启动现场继承的内部控制变量(数据目录重定向、调试端口开关、迁移开关等)一并永久记住:此后每次启动(包括从 Finder 正常打开)都套用这些残留值,最坏时「安装成功」实际写进一个早已删除的临时目录(界面显示已装、实际不可用),且调试端口可能被常开。现在环境探测只捕获登录 shell 自己导出的内容(你在 .zshrc 里配置的 API key 等照常生效),已被污染的旧缓存会在启动时剥离危险项并留痕、随后自动重建干净。
- **「迁移旧安装」不再可能覆盖你自建的同名技能**(REQ-044,需下个签名版本生效)——定制中心的旧安装迁移此前只按名字匹配:如果你自己写过一个与市场条目同名的技能(真机上确实撞到过),「迁移」会用市场版把它覆盖并删除原件。现在迁移候选必须通过来源核验(与 app 打包资产逐字节一致才认定为 alpha 安装物),核验不过的一律不列入、绝不触碰,排除原因记录在日志;宁可漏迁需要你手动处理的项,也不动你的内容。
- **技能市场撤下三个装不上的条目**(REQ-044,需下个签名版本生效)——「MCP 构建指南 / 视觉海报设计 / 品牌规范应用」的内容从未随 app 打包,点安装必然失败(还波及「设计套件」整个是空壳);已从市场撤下(「开发套件」相应瘦身)。后续内容就绪后经远程市场上架,无需更新 app。**S23 补**:上述撤架此前只对断网用户生效——联网时市场清单来自云端,云端那份没同步撤,三个坏条目仍在显示;现已把云端清单一并撤下(云端生效,无需更新 app)。
- **冷启动不再因残留的旧标签页记录整屏卡死**(REQ-014,需下个签名版本生效)——历史版本残留的旧格式「最近标签页」记录、或指向已删除会话的标签页,此前会把整个界面卡死在报错页/「Not found」白屏且无任何恢复入口(只能手工删配置自救)。现在启动时自动清理这类坏记录(main.log 留痕说明剔除原因),正常标签页恢复不受影响;引擎一时连不上时按原样放行,绝不越修越坏。
- **「推理强度 / 权限档」切换偶发误报『切换失败』修复**(REQ-043,需下个签名版本生效)——切换的实现是驱动引擎控件逐档轮转、读回界面文本确认;此前固定等 90ms 读一次,引擎界面更新稍慢就会被误判为「转满一圈没命中」而报失败(实际已切成功)。现在等到文本真实变化再判定,更新慢不再误报、典型切换还更快;真失败(模型没有该档 / 控件无响应)仍如实提示,不装成功。
- **丢弃陈旧本地默认服务器现在留痕并自动清理**(REQ-042,需下个签名版本生效)——REQ-040 的「自动丢弃易失本地默认」此前无声进行,且陈旧记录永留、每次开机重判;现在 main.log 记一行说明原因,并把陈旧记录一次性清除。
- **冷启动偶发卡「无法连接到 Local Server」修复**(REQ-040,需下个签名版本生效)——若历史上把某个「具体端口的本地服务器」设成过默认,之后每次启动都可能连到一个已失效的端口而整屏卡住(内嵌引擎每次用随机端口)。现在这类易失的本地默认会被自动丢弃、回退到当次真实引擎,不再卡死。
- **推理强度对 DeepSeek 等模型接真**(REQ-041,需下个签名版本生效)——此前 DeepSeek(国内版默认模型)的「推理强度」显示的档位与引擎实际不一致,且点选会「切换失败」(根因:该模型档位是英文 low/medium/high,而控件只认中文低/中/高)。现在中英文档位统一,显示与实际一致、可正常切换;模型确实没有的档位会诚实提示,不再假装。
- **操作失败不再无声**(B11,需下个签名版本生效)——多处「点了没反应」的静默失败补上了提示:①侧栏会话右键菜单「分享」以前把生成的分享链接**直接丢弃**(点了像没反应),现在复制链接到剪贴板并弹提示;「删除 / 重命名」网络失败会弹「删除失败 / 重命名失败」,不再让条目诡异复原;②定制中心「内置能力治理」应用/加载失败此前静默(改动没生效却无提示,甚至有覆写现有治理配置之虞),现在会弹错误并保留原状;③自动化「保存 / 删除」失败此前静默,现在弹错误提示。(「复制对话」此前已有提示,不变。)
- **登录失败不再「点了没反应」**(B11,需下个签名版本生效)——浏览器授权后回到 app 若登录没成功(授权被拒 / 回调残缺 / 登录状态过期 / 网络兑换失败),此前只写日志、界面毫无表示;现在弹「登录失败」提示并按原因给出说明。已知边界:回调把 app 从未运行状态冷启动时提示不出现(界面停留在未登录态,再登一次即可)。
- **引擎连崩停止自愈后界面明确告知**(B11,需下个签名版本生效)——内嵌引擎连续崩溃、自动重启放弃后,此前只写日志,界面看着正常但所有会话都失灵;现在侧栏出现常驻红色横幅「引擎已停止运行」+ 错误提示,并带「重试」按钮(重试重新给满自愈机会)。
- **新会话创建失败不再静默落草稿**(B11,需下个签名版本生效)——项目上新建会话失败时此前静默跳到草稿页;现在弹「会话创建失败,已打开草稿」。侧栏会话操作的提示文案(重命名/删除/分享)同批接入中英双语。
- **首页输入 `/` 现在真的会弹出命令菜单了**(REQ-038,需下个签名版本生效)——此前首页占位文案写着「输入 / 调命令」但能力并不存在(用户报 bug)。现在首页与会话页共用同一命令数据源(内置 + 自定义 + 技能生成的命令),`/` 过滤、↑↓ 选择、回车执行/回填与会话页一致;`@` 引用也一并接线(agent + 项目内文件搜索),引用会作为真实上下文随消息发送。中文输入法组合中回车不再误发(补齐 Safari 路径守卫)。
- **会话输入框发送按钮箭头被裁切** —— 箭头字形被挤出圆形按钮底部、贴着输入框边缘被切掉(用户截图)。根因是换肤层与上游图标容器的布局冲突,已修复,箭头恢复居中。(REQ-038,需下个签名版本生效)
- **会话页与首页的 `/` 命令菜单现在是同一个弹框**(REQ-038b,需下个签名版本生效)——此前会话页弹的是引擎自带的全宽菜单,与首页样式/密度不一致(用户反馈)。现在两处使用同一个 alpha 菜单组件:同数据源、同样式、同键盘交互;选中语义不变(自定义命令回填、内置命令直接执行)。
- **会话工具栏时而多出「上下文」文字、侧面板上下文 tab 时而失灵** —— 上下文用量环的注入逻辑会把侧面板「上下文」tab 的按钮误当作用量环搬进工具栏(所以「有的有、有的没有」:取决于该会话是否开着上下文 tab)。已修复:只收养真正的纯图标用量环,并自动清理此前被误搬的按钮。(REQ-038,需下个签名版本生效)
- composer 控件诚实化(C28,需下个签名版本生效):移除「只读」权限档——实测它与「请求审批」行为完全相同,宣称的「禁止写/执行」并不成立(真只读将以引擎 plan 权限档形式回归,REQ-028);「推理强度 effort」菜单明示「预设 · 暂未接入模型推理」,不再暗示影响推理(真接入随 REQ-029)。
- **定制中心「已安装」卸载/更新按钮点了没反应** —— 对技能/Agent/插件/云能力条目点「移除」或「更新」时静默失败(无任何提示),只有 MCP 连接器能卸。修复后四类条目均可正常卸载净除、更新。(仅打包版受影响;真机验收 REQ-016 时发现并修复)

### Added
- **自动化云端档位**(REQ-025/REQ-022,云端已就绪,客户端需下个签名版本):定时任务新增「云端」执行位置——应用不在线也按时在云上运行(调研类任务),打开应用时结果自动拉回项目的 .alpha/runs 留档;创建时明示数据边界(只上传任务描述文本,不上传项目文件)与预算硬帽;云端连续失败会自动停用并在列表显示原因;删除任务即停止云端触发。
- **推理强度真接入**(REQ-029,需下个签名版本生效):输入框的「推理强度」不再是摆设——对支持的模型(Claude Opus/Sonnet、GPT-5.4 Mini)选择 低/中/高 会真实改变发给模型的推理参数;不支持的模型该按钮如实禁用显示「—」,不再假装可选。
- **自动化增强**(REQ-024,需下个签名版本生效):①「standard 可写档」——定时任务可以修改文件、执行常规命令(破坏类命令被权限拦截;启用时有明确风险确认);②「用 AI 解析」——一句话规则解析不出来时,可让模型帮你抽取周期与任务(解析用的临时会话即用即删);③连续失败 3 次自动停用并通知(修复后一键重新启用);④「立即运行」按钮——手动跑一次,不影响原定排程。
- **任意连接器与 Agent 导入**(REQ-033,需下个签名版本生效):定制中心「创建」新增两个入口——「添加自定义连接器」可接入精选目录之外的任何 MCP 服务(粘贴一条 npx 命令或填一个 https 地址即可,密钥加密存储);「导入 Agent」支持 .md 文件导入,Claude Code 格式的社区 agent 会显示逐字段转换预览(能转的转、不能转的如实说明),确认后立即可用。创建页同时注明了各类扩展的跨生态兼容边界。
- **真·只读模式**(REQ-028,需下个签名版本生效):输入框权限档新增「只读」——选中后 AI 从引擎层被禁止修改文件与执行命令(不是界面文案,是真拦截),适合放心地让 AI 分析代码/调研;可随时切回。切换失败会如实提示并回退,不会显示只读却仍可写。
- **扩展市场脱离发版**(REQ-032,云端+客户端配合,客户端部分需下个签名版本生效):定制中心的目录改为从官网(alphacodeone.com)拉取——官方上架新技能后,进入定制中心即可看到并安装,不再需要等 app 发新版。目录带数字签名(被篡改会整体拒用并回退),下载的技能文件逐个校验指纹(不匹配拒装);断网时回退到内置目录,市场永不空白。首个远程上架技能:「规范化提交信息」。
- **官网新手指南**(REQ-026,已上线):alphacodeone.com/getting-started——从下载安装、登录到第一次对话的完整图文步骤,以及 10 条常见问题解答(登录无反应/额度不足/模型列表/权限审批/数据安全等),不需要任何技术背景。
- **内置能力治理**(REQ-037,需下个签名版本生效):定制中心「已安装」新增「内置(上游)」分组——可对引擎原生 agent(build/plan/general/explore)隐藏或禁用、以自己的提示词重写,可禁用内置技能(禁用后斜杠菜单仍会列出——引擎行为——但执行被拦并给出诚实说明),可用自己的模板重写内置 /init /review 命令;支持黑名单/白名单两种模式与一键重置。承重项(如自动压缩)受保护不可禁用,并说明原因;全部改动免重启生效。
- 侧栏项目治理(B4,需下个签名版本生效):根目录(`/`)与整个家目录(`/Users/你`)不再被当作项目纳入侧栏与取数——它们几乎不可能是有意的项目,却各挂一整棵目录树的文件监听与扫描;「归档」项目现在真正零请求(此前只是视觉隐藏,后台照常取数建监听)。已知限制:恢复已归档项目暂无界面入口(可清 `alpha.sidebar.hidden` 本地存储)。高级:引擎实验 file-watcher 现在可用 `export OPENCODE_EXPERIMENTAL_FILEWATCHER=false` 关闭(此前被硬覆盖关不掉;默认仍开,负责外部编辑/外部 git 操作的自动刷新)。
- 界面崩溃局部降级(C28,需下个签名版本生效):alpha 自有界面(侧栏/首页/定制中心/自动化面板等 10 处)任一区域崩溃时,不再整屏坠成错误页——只降级该区域并出现右下角「重载此区域」浮条,应用其余部分照常可用。
- 会话数据库安全带上线(C17+B14,需下个签名版本生效):
  - **升级前自动备份**:app 升级后首次启动、引擎将要迁移会话数据库时,先自动做一份已验证的快照(滚动保留 5 份,存于应用数据目录 `alpha-db-backups/`),留出降级逃生路。
  - **版本过新诚实拦截**:用旧版 app 打开被更新版本迁移过的数据库时,启动前弹窗如实说明(不再静默继续跑在不理解的数据上),可选退出升级 / 备份后继续 / 直接继续。
  - **损坏可恢复**:数据库损坏时启动不再只是「服务启动失败」——弹窗指引从最近备份一键恢复,损坏文件改名保留不删除。
  - **「数据」菜单**:新增应用菜单项,可随时手动备份、导出整库到任意位置(导出件经完整性验证)、打开备份文件夹。
- 自动化定时任务上线(REQ-021 A1,需下个签名版本生效):侧栏新增「自动化」——一句话描述(如「每天早上 9 点,检查本项目未处理的 TODO 并生成清单」)自动解析出周期,逐字段确认后保存,到点自动在指定项目里执行并把报告写进 `<项目>/.alpha/runs/`,完成弹系统通知、失败在侧栏亮红色角标:
  - **只读安全档**:自动任务只能读文件/检索/联网查询,不能改文件、不能执行命令,`.env` 类文件一律不读;全程零弹窗(无人值守设计),可写档后续版本提供。
  - **诚实边界**:任务只在应用运行时执行(不是后台常驻服务),界面明示;配套「登录时启动 alpha-code」开关;睡眠/关机错过的触发按策略跳过并留记录,不静默丢失。
  - **护栏**:单任务超时中断(默认 15 分钟)、全局每日运行上限、同一时刻只跑一个任务(被跳过的有记录);任务历史可回跳查看完整会话原文与 run 产物。
- 定制中心 M3 · 云能力分区上线(REQ-020,需下个签名版本生效):
  - **「云能力」tab 从占位变真内容**:平台连接器卡(实时连接状态)+ 三条云 pipeline(云代码审查 / 深度调研 / 文档生成);未登录或 BYOK 模式下条目灰显并如实说明原因,一键登录/切换平台模式点亮。
  - **每条云能力有完整介绍页**:输入契约、预算默认与上限、执行层、**上行数据明细**(送出去什么一目了然);云连接器详情列出 4 个云派发工具与数据边界。
  - **「启用」不动引擎配置**:只把 pipeline 加入本机可用列表(安装账本),供会话与自动化选用,停用即从「已安装」移除。
  - **云代码审查可从详情页直接派发**:选项目 → 自动取 git diff(只传 diff,绝不上传全库)→ 云端执行进度实时显示 → 完成后结果自动回流项目 `.alpha/runs/`。
- 云派发数据安全三道闸(ADR-021,对会话内 agent 派发与界面派发同一入口生效):派发信封超过 1MB 直接拒发(不静默截断);diff/目标文本里检测到密钥样式内容(API key/私钥块/token)拒发并指出具体字段;未声明保护路径时自动注入 `.env* / *.pem / .alpha/ / .git/` 排除清单。
- 定制中心 M2 · 横向导航 + 详情页(REQ-019 T1/T2,需下个签名版本生效):
  - **九个横向类目 tab**(推荐/连接器/技能/Agent/插件/套件/已安装/创建/云能力);「已安装」tab 带琥珀角标提示可更新数,可更新条目在列表顶部单列分组。
  - **每个条目有了介绍页**:点卡片进入详情(简介/许可证/版本/数据边界/运行时依赖/所需密钥),`待核实` 条目如实标注;`Esc` 逐级返回(弹框 → 详情 → 列表 → 关闭)。
  - **按类型分档的安装路径**:技能点「添加」即装即生效(免弹框);连接器/套件走确认框(密钥密文采集/组合清单);插件先看介绍页再安装,并明确提示「运行于引擎进程内」的风险。
  - **全局搜索**:切换类目不清空,输入即出跨类目分组结果;来源 / 许可证一键筛选。
  - **详情页逐类型内容**:连接器列「提供的工具」清单与数据边界(远程列目的主机、本地如实说明联网取决于所连服务);技能全文预览 SKILL.md;Agent 显示权限档摘要与系统提示预览;插件列 hooks 与「插件 ≠ 套件」说明;套件组合清单可逐项查看、逐项安装重试。
  - **进详情页即检测运行时依赖**(uv / node / git 实时探测),缺失直接给安装指引,不用等点「添加」才发现。
  - **更新通道**:目录钉版更新后,「已安装」tab 角标提醒,可逐条或全部更新(连接器更新走确认框以便重填密钥,不静默丢失)。
  - **导入**:本地文件夹(校验 SKILL.md 后入库)、Git 仓库(https 浅克隆校验)、npm 插件包 —— 全部落 `.alpha` 并记账,来源不明内容一律不执行。
  - **离线安装(供给链)**:官方推荐插件随 app 预打包,安装写绝对路径、全程零网络(弱网/断网也能装);新增官方只读「代码审查」Agent 一键安装。
  - **安装反馈**:检查依赖 → 安装中分阶段提示;失败一律行内展示(卡片 / 详情页),不再只弹一闪而过的提示。
- 定制中心全面升级(全类型通用化,REQ-018,需下个签名版本生效):
  - **装完即用、免重启**:安装技能 / Agent / 插件后,引擎自动重载,**当前会话下一条消息**就能用上——不再需要重启应用(此前技能/Agent 写盘后其实不生效);npm 型插件首次启用会后台下载依赖。
  - **新增 Agent 分区**:浏览内置 Agent(build/plan 等)与你自建的 Agent,一处创建、一处管理。
  - **「已安装」覆盖全部类型**:连接器 / 技能 / Agent / 插件统一列出,可查看类型、版本、状态,一键卸载(此前只有连接器可管、其余装完失管)。
  - **连接器密钥安全化**:安装需要密钥的连接器(GitHub / 飞书 / 语雀等)时可直接填密钥,密钥存本机安全文件(0600)、配置里只留引用**绝不明文**;卸载即吊销(此前根本没有密钥输入框,需 key 的连接器装了也不能用)。
  - **安装落点改为 alpha 自有目录**:技能 / Agent / 连接器 / 插件统一装进 `.alpha`(经软链桥接被引擎发现),不再污染你自建的 opencode 配置;检测到旧版安装可一键迁移(迁移开关随后续真机验证开启)。
  - 内置官方「skill-creator」技能(Apache-2.0)随包分发。
- 云任务结果自动落到项目里:agent 在会话内跑完云任务(cloud_* 工具)后,任务状态与产物自动写入项目根 `.alpha/runs/<任务id>/`(status/contract/artifacts),完成时右下角提示;`.alpha/` 自带 gitignore 不污染仓库(B3/ADR-019,需下个签名版本生效)
- sidecar 引擎崩溃自动恢复:引擎进程意外退出后 10 秒内自动重拉(指数退避,连续崩溃 5 次后停手防风暴);恢复未确认健康前不再刷新界面(防白屏)(B5)
- alpha 自有后端扩展开始随包分发:内置 `alpha_ping`/`alpha_echo` 工具(agent 工具表可见),是后续 alpha 自有工具/能力线的装载通道(B6/G1,需下个签名版本生效;`ALPHA_EXT_DISABLE=1` 可关)
- 模型目录按版本显隐(国内版/国际版):平台代理与内置「自带 Key」目录由网关白名单驱动,登录或打开模型面板即同步、无需更新 app;离线时回退内置目录并显示「内置目录」徽标;用户自定义添加的节点不受版本限制(REQ-001)
- 登录态自动续期:登录一次长期有效——token 到期前自动轮换刷新,不再出现「用着用着平台模型突然 401/要求重登」;仅当会话在网页端被撤销时才要求重新登录(B2,需下个签名版本生效)

### Changed
- 首页整洁化:移除 composer 下方与左侧栏重复的项目/会话快捷条(该黄金位留作后续功能入口);工作区切换 chip 与左侧栏项目导航保留(REQ-011,需下个签名版本生效)

### Performance
- 启动更快:sidecar 健康探测改为立即首查(此前每次启动固定先 sleep 100ms 再探测),常规启动约省 100ms(D1,需下个签名版本生效)

### Fixed
- 失败不再静默:项目列表加载失败在首页/侧栏均有明确提示与重试;定制中心「已安装」读取失败有顶部错误条;账户信息读取失败不再误显「钱包按量扣费」(改为「读取失败 + 重试」);全局 `opencode.jsonc` 写坏(语法错误或不认识的配置项)时首页给出「配置未生效」警示并可一键打开文件——此前引擎会静默忽略整份配置;启动等待期增加状态文字(B11/B23/B20)
- 云任务进度流更抗断:断连/空转改指数退避重连(不再风暴)、断点续传保真(非数字事件 id 不再丢)、任务结束判定更健壮、僵死连接 90 秒自动回收(REQ-003/C23)
- 启动提速:不再每次启动都等待登录 shell 探测(.zshrc 里 nvm/conda 慢时最坏 ~10s 黑屏)——上次结果缓存即开即用,探测转后台刷新(B1,首次启动仍会探测一次)
- BYOK 改密钥/删密钥即时生效:不再需要重启 app——改完立刻用新 key 调用,删除立刻吊销(此前旧 key 会滞留到重启)(B21)
- 界面回归根治:此前每日上游同步会静默打断 alpha 的界面定制(用户消息/工具卡/发送按钮/新对话等多处样式回落),已将上游前端冻结在 6/30 验证过的状态、每日同步只进引擎——同类回归物理上不再发生(REQ-013/ADR-020;界面恢复随下个版本生效)
- 平台代理用量计量:流式对话(OpenAI-wire)的 token 用量此前**全量丢失**、账户用量恒为 0——已修复,登录后经平台模型的每次对话都会正确计入今日/本周用量与套餐窗口(REQ-002 BP-1,网关侧)
- 冷启动登录态:app 重启后不再错误显示「未登录」——已存储的登录凭证在启动时可靠恢复(REQ-002 BP-2,需下个签名版本生效)

### Security
- 应用加固:关闭 Electron 的三个代码注入原语(RunAsNode/NODE_OPTIONS/调试注入)、开启 asar 完整性校验;移除签名沙箱的 dylib 注入豁免组合;「用指定应用打开」收紧为编辑器白名单(白名单外改用系统默认打开)(C27/C25,需下个签名版本生效)
- 界面进程数据外传通道收紧:此前任何跨域响应都被强制放行读取(`ACAO:*`),叠加无 CSP,一旦渲染进程被注入即可向任意外站发请求并读回响应;现改为仅本机引擎(回环)放行 + 打包版注入内容安全策略(网络连接仅限本机;`ALPHA_CSP_DISABLE=1` 可临时关闭排障)(C24,需下个签名版本生效)
- 自动更新不再允许降级:防止更新源被替换/重放旧版本把 app 打回含已修漏洞的版本(需要旧版时手动下载对应 Release 的 dmg)(B9)
- **第三方 MCP/LSP 不再继承你的密钥**:此前任何安装的连接器子进程都能从环境变量读到平台登录凭证与全部 BYOK 模型密钥;现改为白名单环境 + 文件通道,密钥不再进入子进程环境(附带:登出即时吊销密钥文件;`echo $ALPHA_API_KEY` 在 agent 终端输出为空属预期)(A6,需下个签名版本生效)
- websearch 改为始终走 keyless 公共端点(限流版):`EXA_API_KEY` 不再透传给引擎——自有 Exa key 用户如需恢复,设 `ALPHA_ENV_ALLOWLIST_EXTRA=EXA_API_KEY`(A6 附带)
- 定制中心内置连接器目录钉精确版本:一键安装的 MCP 不再每次在线解析 `latest`,消除版本漂移与供应链不确定性(钉值 = 更新当日各包 latest,行为一致仅冻结确定性)。存量已装配置的一键迁移随定制中心升级落地(迁移时按钉版重装),迁移开关随后续真机验证开启(A2/REQ-018)

## [0.1.0] - 2026-07-03

**首个签名 + 公证的对外发布**(Developer ID + Apple 公证,`stapler validate` / `spctl: Notarized Developer ID` 双过;自动更新 feed 指向自有仓库)。

### Added
- alpha 品牌 Mac 应用:prod 渠道 `productName=alpha-code`、appId `com.tide.alphacode`、深链 `alpha-code://`(C18 / A5 / ADR-012 / ADR-017)
- 自有前端接管(ADR-016):AlphaHome 首页、侧栏、composer、模型选择器、设置、alpha-ui 设计系统
- 定制中心:MCP 浏览 / 一键安装 / 启停、skill / agent 创建、plugin 安装、内置技能种子(ADR-014)
- 平台账户体系:浏览器授权登录(PKCE 深链)、平台代理模型(platform-pays)、BYOK 钥匙串管理(ADR-017)
- 关于面板 + MIT NOTICE 随包分发(B15)
- websearch 对所有 provider 默认放开(ADR-009)

### Fixed
- 启动性能:窗口先行、定制中心惰性加载、渲染层取数减半(A1 / A2-部分 / A3,PR #22–#26)
- 内嵌 server 版本 `local` → 真实 npm 版本:带 `.opencode` 插件的项目首个请求不再被必败安装阻塞(A4,PR #33)
- 鉴权生命周期:过期重登后代理恢复、登出清 token 不串台(A8,PR #29)
- 崩溃屏去 OpenCode 品牌、版本不再显示 0.0.0(C29 / A5)

### Security
- 深链日志脱敏、外链 scheme 白名单、MCP 配置写入校验(反注入)、端点 https 守卫、IPC 导航 / store 硬化(C11 / C13 / C2 / C26 / C1,PR #22 / #25)
