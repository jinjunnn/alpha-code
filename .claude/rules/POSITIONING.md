# 产品定位(POSITIONING)

> 最后更新:2026-07-05(REQ-008 定位连拍收口:团队协作/企业租户不做、非技术用户正式纳入(分期文档先行)、前 2-3 功能收口;详见 docs/debates/2026-07-05-req008-positioning-briefs.md)
> 回顾节奏:每次 `/app:retro` 时审视是否仍有效
> 说明:架构相关条目已确认;标 `〔待补〕` 的是产品层判断,需你确认/修正。
> 2026-06-18 修订:由"个人工具"转向**面向多用户分发 + 云端多租户**(见 ADR-010/011)。

## 一句话定位
alpha-code 是一个**基于 opencode 的 Mac 编码 agent 产品(面向多用户分发)**:在**不改 opencode 源码**的前提下,叠加**自有独立 UI**、**自有后端能力**,以及一个**面向所有租户的云执行平台**(把非编码任务派到云上跑、返回结果,见 ADR-010/011);同时**无成本继承 opencode 的每次 upstream 升级**。

## 目标用户(画像化,不抽象说"用户")
- **角色**:深度 AI 编码工具使用者 / 开发者(熟悉 opus + multi-agent 协作的 power user 为**主画像**)。**已从"仅作者本人"转向面向多用户**:每人在各自 Mac 跑自有实例,云平台为所有租户共享(多租户)。**第二画像(2026-07-05,REQ-008 D3 拍板)**:**非技术用户(小白)**——经「登录即代付」零配置起步;当前阶段以规范文档服务(REQ-026),新手引导/支持面等重投入分期后议。
- **日常**:在 Mac 上用编码 agent 跑多项目;对 UI 展示逻辑与后端能力有强烈的个性化诉求。
- **当前痛点**:opencode 官方 Mac 前端的展示/交互不是"我的";想要自己的 UI 和若干自有后端能力,但**又不愿 fork 死**——一旦改源码,upstream 升级就接不住。
- **不是谁(2026-07-05 修订,REQ-008 D1/D2/D3 拍板)**:~~不主打小白~~ → **非技术用户(小白)已正式纳入目标**(分期服务,见上「第二画像」);**不做团队协作**(共享 workspace/会话)——多租户 = 多个独立租户共享云平台;**不做企业租户**(合同/SSO/合规;重评触发 = 真实付费企业线索或 C 仓 license 体系成熟)。

## 解决什么具体问题
1. **隔离扩展**:在 opencode 之上加自有后端能力(工具/hooks/MCP/sidecar),**零改动**原仓库源码。
2. **独立前端**:做一套自己的 Mac UI(自有审美 + 优化过的展示逻辑),而不是被官方 UI 绑死。
3. **升级可继承**:opencode 升级后,只通过 `@opencode-ai/sdk` / `@opencode-ai/plugin` 两个契约层对接,自定义层不冲突。

## 明确不解决的问题
- 不解决 opencode 本身的 agent/会话/上下文引擎问题——那是上游的,直接白嫖。
- 不解决跨平台(web/tui/console/enterprise)——只聚焦 Mac desktop。

## 竞品 / 替代品
- 直接用官方 opencode desktop(放弃定制)。
- 硬 fork opencode 改源码(放弃升级继承)——alpha-code 正是要避免这条路。
- 从零自研编码 agent(放弃 opencode 成熟的 agent core)。

## 我们的差异化
**后端隔离架构 + 前端接管**:**后端**定制只发生在 opencode 官方扩展接缝(`@opencode-ai/plugin` hooks、`.opencode/*` 文件、MCP、sidecar)上,守升级隔离;**前端**由 alpha **自有组件全面接管**(ADR-016,经 `@opencode-ai/sdk` 取数,放弃前端升级隔离)。opencode 源码作为 **fork 上游只读**(非 submodule,ADR-005);升级 = `git merge dev` + 两个契约包 diff 适配。

## 〔已收口〕历史待确认项(2026-07-05 全清)
- ~~纯个人工具 vs 对外发布~~ → **已定(2026-06-18):对外发布、面向多用户 + 云端多租户**。连带待定:认证授权、租户隔离边界、配额/计费、LLM key 归属(平台付 vs BYOK)、滥用防护(见 ADR-010/011)。
- ~~是否做**团队协作**~~ → **已定(2026-07-05,REQ-008 D1):不做共享 workspace/会话**,只"多个独立租户共享云";E13 rejected(重开条件 = 真实付费团队需求 + 上游多用户原语)。
- ~~"自有后端能力"和"前端优化"的**前 2–3 个具体功能**~~ → **已定(2026-07-05,REQ-008 D4/D5)**:后端 = ①云派发 pipeline ②自动化定时任务 ③扩展供给链/连接器生态(第 4 候补 = B14+C17 DB 安全带);前端 = REQ-005 清单收口。详见 GOALS。
- 决策过程与全部结论:`docs/debates/2026-07-05-req008-positioning-briefs.md`。
