---
id: D6
title: userData 每启动新建 log 目录(增生观察)
type: debt
priority: P3
status: rejected
repo: A
created: 2026-07-03
sprint: —
source: 册 §一 P3
---

## 背景/证据
userData 每次启动新建 log 目录;已有 7 天清理逻辑,增长有界性待观察。

## 验收标准
1. 观察一个周期(≥7 天日常使用)后记录结论:7 天清理是否足以有界;
2. 不足则改(合并到单目录/按天滚动);足够则关闭本条并记录。

## 关联
C3(日志治理同域,可同批)。

## 评估(2026-07-04,/loop — 机制核实;≥7天观察是真机任务)
代码核实 `logging.ts`:每启动建 `<userData>/logs/<stamp>/`(:108-110,`stamp()`=本地时间戳);**清理机制存在且有界** —— 每次 init 扫 `<userData>/logs`,删 mtime 早于 `MAX_LOG_AGE_DAYS` 的旧目录(:126)。
- **判断**:per-run 目录是**有意设计**(每次运行日志隔离,便于事后取证 / `exportDebugLogs` 按窗口收集),非缺陷;7 天滚动清理为其配套上界。**无干净可改点** —— 盲目「合并到单目录」会破坏 per-run 隔离,属设计变更而非修复。
- **验收①(观察≥7天有界性)= 真实使用任务**,离线/单会话不可代跑;机制上已有界,倾向验收②的「足够则关闭」。→ 保持 registered,待真实使用一周期后按盘面确认关闭,或实测增生失控再议按天滚动。
